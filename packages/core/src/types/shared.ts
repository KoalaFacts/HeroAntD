/**
 * Ant Design Shared Types
 *
 * Common types from React Ant Design for use in Stencil components.
 * Add new types as needed when porting components.
 */

// =============================================================================
// COMMON
// =============================================================================

export type SizeType = 'small' | 'middle' | 'large';
export type Variant = 'outlined' | 'borderless' | 'filled' | 'underlined';
export type InputStatus = 'warning' | 'error' | 'success' | 'validating';

// =============================================================================
// LAYOUT
// =============================================================================

export type DirectionType = 'ltr' | 'rtl';
